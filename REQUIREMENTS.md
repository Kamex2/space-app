# 3D太陽系エクスプローラー 要件定義書

作成: Claude Fable 5 / 実装担当: Claude Opus 4.8
プロジェクトルート: `C:\Claude\Fable5\solar-system-explorer`（このファイルがあるディレクトリ）

## 1. 概要

ブラウザで動く3D太陽系エクスプローラー。JPLの近似惑星軌道要素を使って任意の日付（1800〜2050年）の惑星配置を物理的に正しく再現し、さらに探査機を発射してスイングバイ（重力アシスト）軌道を設計して遊べるモードを持つ。

デモやAPIキーは不要。すべての計算はクライアント側で完結し、オフラインで動作すること。

## 2. 技術スタック（固定）

- Vite + TypeScript（テンプレート: `vanilla-ts`）。React等のUIフレームワークは使わない。
- Three.js（npm の `three` + `@types/three`）。OrbitControls、必要なら `three/examples/jsm` のポストプロセス（UnrealBloomPass）を使用可。
- 外部アセット（テクスチャ画像、CDN、Webフォント）は一切使わない。マテリアルは色・emissive・簡単なプロシージャル表現で構成する。
- UIラベルはすべて日本語。コード識別子は英語。

## 3. 機能要件

### F1. エフェメリス計算エンジン（最重要・精度の要）

- JPL "Approximate Positions of the Planets"（Standish, https://ssd.jpl.nasa.gov/planets/approx_pos.html）の Table 1（1800 AD – 2050 AD 有効）のケプラー軌道要素を使う。**必ずWebFetchで上記ページ（またはリンク先のaprx_pos_planets.pdf / p_elem_t1.txt）を取得し、8惑星（EM Barycenter含む）の a, e, I, L, ϖ, Ω とその世紀あたり変化率を正確に転記すること。** 記憶からの転記は誤植リスクがあるため不可。取得した数値はソース内の定数テーブル `src/data/planetElements.ts` に、出典URLコメント付きで格納する。
- 計算手順（JPL文書の手順に厳密に従う）:
  1. J2000からのユリウス世紀数 T を計算し、各要素を `elem = elem0 + rate * T` で更新
  2. 平均近点角 M = L − ϖ（[-180°, 180°] に正規化）、近日点引数 ω = ϖ − Ω
  3. ケプラー方程式 M = E − e·sin(E) をニュートン法で解く（許容誤差 1e-8 rad、初期値 E = M + e·sin(M)）
  4. 軌道面座標 → 黄道座標（J2000黄道・春分点基準）への回転変換（ω, I, Ω）
- 出力: 太陽中心・黄道座標系の位置 [AU]。速度も必要（スイングバイモードで使用）。速度は解析微分（dE/dt = n / (1 − e·cosE) 経由）で求める。数値微分でも可だが精度を落とさないこと。
- 対象天体: 水星・金星・地球（EM barycenter）・火星・木星・土星・天王星・海王星の8惑星。

### F2. 3Dシーン

- 太陽（emissive + 控えめなbloom）、8惑星、各惑星の軌道線（軌道要素から楕円を直接生成、現在日付の要素で描画）。
- 背景は星空（乱数配置のPoints、数千点）。
- 距離スケール切替: 「実スケール」と「圧縮スケール」（r' = r^0.6 など、外惑星が見える圧縮写像）をトグルできる。軌道線・惑星位置・探査機軌道すべてに一貫して同じ写像を適用すること。
- 惑星サイズは視認性のため誇張表示（デフォルト）。「実寸比」トグルで実比率にできる。
- 各惑星に日本語ラベル（CSS2DRendererまたはスプライトでカメラ正対）。遠い惑星でもラベルは読めること。
- カメラ: OrbitControls。惑星クリックでその惑星にフォーカスし追従。ダブルクリックまたは「太陽に戻る」ボタンで解除。

### F3. 時間制御

- 表示範囲: 1800-01-01 〜 2050-12-31（Table 1の有効範囲）。
- UI: 日付表示（YYYY年MM月DD日）、日付入力（`<input type="date">`）、再生/一時停止、速度スライダー（1日/秒 〜 1年/秒、逆再生も可）、「今日」ボタン。
- 再生中は惑星がなめらかに公転する（毎フレーム、エフェメリスをフル再計算して問題ない計算量のはず。60fps維持）。

### F4. 惑星情報パネル

- フォーカス中の惑星について: 太陽からの現在距離 [AU]、公転周期、離心率、軌道傾斜角、現在の日心黄経を表示。値はすべて適切な桁数に丸める。

### F5. スイングバイ設計モード（**2026-07 削除済み**）

> ユーザーの要望により設計モード（手動スイングバイ＋ポークチョップ＋発射・記録・探査機テレメトリのUI）
> は削除した。`src/sim/swingby.ts`・`src/sim/porkchop.ts`・`src/ui/porkchopPanel.ts` と SolarScene の
> 軌道プレビュー系メソッドは残してあるが、UIからは呼ばれていない。復活させる場合は git 履歴ではなく
> 以下の仕様を参照して再配線すること。

<details>
<summary>旧仕様（参考）</summary>

- モード切替ボタンで通常モード⇔設計モード。
- 探査機の打ち上げ: 現在日付の地球位置から出発。ユーザーは以下をスライダーで指定:
  - 出発余剰速度 v∞: 0.5〜16 km/s
  - 方向: 黄道面内角度（地球公転速度ベクトル基準 −180°〜+180°）と面外角度（−45°〜+45°）
  - 初速 = 地球の公転速度ベクトル + v∞ベクトル
- 軌道伝播: 太陽+8惑星の重力を含むN体（探査機はテスト粒子）。積分はRK4固定ステップ（基本 0.5日、惑星から0.1 AU以内では 0.02日以下に細分）。単位系は AU・日で統一（換算: 1 km/s = 5.7755e-4 AU/day。GM値は §5 参照）。
- スライダーを動かすたびに、その場で最大15年分の予測軌道を再計算して線で描画する（Web Worker化は不要だが、60fpsを大きく損なう場合は計算を間引く/デバウンスする）。
- フライバイ検出: 惑星に 0.05 AU 以内まで接近したら軌道上にマーカーと「◯◯スイングバイ YYYY/MM/DD、通過距離、速度変化 Δv」を表示。太陽中心速度の増減が分かること。
- 「発射」ボタンで確定すると、時間再生に同期して探査機が軌道上を移動する。探査機にもラベルと軌跡を表示。
- 遊びの目標として、パネルに各外惑星への「最接近距離」記録を表示（例: 木星まで 0.03 AU まで接近!）。

</details>

### F6. UI/HUD

- 画面端に半透明ダークのHUDパネル（上: 日付と時間制御、右または左: モード・スケール切替と情報パネル、下: スイングバイ設計スライダー群）。CSSは手書きで良いがモダンで統一感のある見た目にする（フラット、過剰装飾なし）。
- 操作説明を初回に小さく表示（ドラッグで回転、ホイールでズーム、クリックでフォーカス）。

## 4. 非機能要件

- 60fps目標（フルHD、統合GPU想定）。ジオメトリ再生成を毎フレーム行わない（軌道線は日付変化が大きい時のみ更新、探査機予測線はパラメータ変更時のみ再計算）。
- `npm run build` がエラーゼロで通ること。`tsc` の型エラーもゼロ。
- モジュール分割: `src/data/`（軌道要素・物理定数）、`src/ephemeris/`（ケプラー計算）、`src/sim/`（RK4伝播・フライバイ検出）、`src/scene/`（Three.jsシーン構築）、`src/ui/`（HUD）、`src/main.ts`。

## 5. 物理定数（この値を使うこと）

- GM_sun = 2.9591220828559093e-4 AU³/day²
- 惑星のGMは太陽との質量比から算出: 水星 1/6023600, 金星 1/408523.71, 地球+月 1/328900.56, 火星 1/3098708, 木星 1/1047.3486, 土星 1/3497.898, 天王星 1/22902.98, 海王星 1/19412.24
- 1 AU = 1.495978707e8 km、1 km/s = 5.7755e-4 AU/day

## 6. 受け入れ検証(実装完了前に必ず実施し、結果を報告に含める)

1. `npm run build` 成功。
2. サニティテスト(`npx tsx` またはviteのnodeスクリプトで実行できる `scripts/sanity.ts` を作る):
   - 全惑星・全年代（1800/1900/2000/2050の各1月1日）で |r| が近日点距離〜遠日点距離の範囲内
   - 地球: 1月上旬に |r| ≈ 0.983 AU（±0.002）、7月上旬に ≈ 1.017 AU（±0.002）
   - 水星の公転周期 ≈ 88日（連続する近日点通過から確認、±1日）
   - ケプラー方程式ソルバが e=0.2, M=全域 で残差 < 1e-8
3. スイングバイ: v∞やRK4のエネルギー保存チェック（惑星接近がない2体区間で軌道エネルギーの相対誤差 < 1e-6）。

## 7. 実装しないもの（スコープ外）

- 月・小惑星・冥王星、テクスチャマッピング、実照度シミュレーション、セーブ機能、モバイル最適化、多言語切替。

## 8. 大宇宙エクスプローラー拡張（Fable 5 の想像する宇宙）

太陽系の外へ出て、天の川銀河・近傍の恒星系・他銀河・星雲・多元宇宙までを探索できる
「大宇宙モード」。太陽系シーンとは独立した Three.js シーンとして実装し、既存機能は一切変更しない。

- **入口**: 画面上部の「🌌 大宇宙へ出発」ボタン。押すと大宇宙シーンへ切り替わり、天の川全景から
  太陽系へゆっくりワープする導入演出が入る。「☀ 太陽系へ帰還」で元の太陽系シミュレーションへ戻る。
- **カタログ（`src/cosmos/cosmosData.ts`）**: 26 の天体を 5 領域（太陽近傍／天の川銀河／局所銀河群／
  深宇宙／多元宇宙・異界）に分類。実在天体（アルファ・ケンタウリ、シリウス、TRAPPIST-1、
  ベテルギウス、アンドロメダ銀河、ソンブレロ銀河 等）には実際の距離と解説を、Fable 5 の空想天体
  （琥珀の環世界、二重らせん星系ヴェスペル、螺旋の楽園アウレリア、逆行宇宙レトロヴァース、
  はじまりの一点オリジン 等）には物語上の距離と創作解説を持たせる。実在／空想はバッジで区別。
- **描画（`src/cosmos/CosmosScene.ts`）**: 種別ごとに手続き生成。渦巻／楕円／不規則銀河はパーティクル、
  星雲は多層の加算合成点群、恒星系は主星＋公転する惑星、特異天体は発光核＋光の環＋渦ハロー。
  外部アセットは使わず、色とジオメトリのみで構成（既存方針を踏襲）。
- **航行**: 目的地カタログまたは天体クリックで選択 →「ワープ」でカメラが対象外まで飛ぶ（星が流れる
  ワープ演出つき）。WASD＋QE で自由航行（Shift 加速）、ドラッグ回転・ホイールズームも可。
  到達した天体は「航行記録」に追記される。
- **UI（`src/cosmos/cosmosHud.ts`）**: 左に航行チャート、右に解説パネル、右下に航行記録、上部にタイトルと
  帰還ボタン。既存 HUD と同じダークテーマ・CSS 変数を使用。
- **未知の銀河（探索の余白）**: curated 26天体の外側に、決定論的に手続き生成した 48 の「未確認深宇宙銀河
  （UDG）」を深宇宙〜多元宇宙の殻にばらまく（`generateUnknownGalaxies`）。チャートには載せず、飛んで/
  クリックして/「🔭 未知の銀河をスキャン」で発見する。名前・かたち・色・距離・解説は乱数から生成され、
  「宇宙はもっと広く、まだ見ぬ場所がいくらでもある」ことを体感させる。
- **航行計器**: 上部に最寄り天体名と発見数をリアルタイム表示。「🔭 スキャン」は現在地から最も近い
  未発見の UDG を捕捉してワープする（発見の連鎖ができる）。
- **展示モード（自動探索ツアー・2026-07 追加）**: アプリは**起動直後から大宇宙エクスプローラーを表示**
  （太陽系へは「☀ 帰還」でいつでも戻れる）。自動探索はデフォルト ON（「🛰 自動探索」ボタンでトグル）:
  30秒ごと（`TOUR_INTERVAL_MS`）に最寄りの未知の銀河へ自動スキャン→ワープし、解説パネルと航行記録も
  連動更新。**10回ワープしたら引きの全景**（`warpToOverview`、カメラ (220,900,1600)→原点）へ戻り、
  周回を繰り返す。来場者が操作（pointerdown/wheel/keydown）すると一時停止し、**60秒**触られなければ
  再開（`IDLE_RESUME_MS`）。シアター/ダイブ/太陽系のまま**3分**放置されたら自動で宇宙探索へ復帰
  （`IDLE_RESET_MS`）。計器に「次のワープまで◯秒（n/10）」を常時表示。展示会での無人運転を想定。
- **銀河衝突シアター（`src/cosmos/collisionTheater.ts`）**: 天の川×アンドロメダの衝突・合体を制限多体
  シミュレーション（Toomre & Toomre 流：星はテスト粒子、銀河核は相互重力＋力学的摩擦）でライブ上演。
  約1.1万粒子、セミインプリシット・オイラー積分（DT=0.06、フレームあたり最大24サブステップ）。
  潮汐の尾はスクリプトでなく計算から創発する。時間換算は数値検証で較正済み
  （OKU_YEARS_PER_UNIT=0.73 → 最初のすれ違い≈46億年後・完全合体≈69億年後、実際の予測と整合）。
  HUD: 億年カウンター・核間距離・再生/一時停止・スピード(0.2〜5×)・リセット・閉じる。
  上演中はチャート等を隠し、クリック/WASD/ワープを無効化。合体判定は `merged` フラグで恒久保持。
- **ブラックホール・ダイブ（`src/cosmos/blackHoleDive.ts`）**: いて座A*（カタログ天体 `sgr-a`、実在）へ
  宇宙船で降下する教育コンテンツ。表示物理量はシュヴァルツシルト解に基づく実計算
  （時間の遅れ γ=1/√(1−rs/r)、潮汐 Δa=2GM·h/r³、いて座A*の質量430万太陽質量で較正済み。
  γ(1.5rs)=1.733=√3 を検証済み）。降下は r/rs の指数スクリプト（400rs→1.02rsを約120秒）、
  7段階のナレーション（降着円盤・ドップラービーミング／時間の遅れ／ISCO／光子球／
  潮汐が弱い＝巨大BHの意外な優しさとスパゲッティ化の対比／地平線直前の見え方）を距離で発火。
  地平線到達で暗転→最終カード（もう一度／宇宙へ帰る）。視覚は黒球＋光子リング＋レンズ風アーチ＋
  ドップラービーミング付き降着円盤（近づく側が増光・青方偏移、毎フレーム色計算）。
  入口は上部「🕳 ブラックホールダイブ」ボタンと、いて座A*の解説パネル内「ブラックホールに飛び込む」
  （「BHダイブ」等の略記はNG——ユーザー要望でフル表記に統一）。シアターとは相互排他。
  ライブ計器: r/rs・γ・潮汐力・あなたと地球の経過時間（γ積算）。ダイブの説明パネルは**画面左端**
  （left:16px・幅300px）——中央に置くと没入感を損なうというユーザー要望による配置。
- **明るさの方針**: ブルーム・グローは初期実装の値（cosmos 0.85/0.5、太陽系 0.7/0.82、グロー0.8）。
  一度暗くしたがユーザーの希望で元に戻した経緯あり——勝手に変えないこと。
- **ブラッシュアップ（2026-07-19）**:
  - **シネマ字幕（`.cosmos-cinema`）**: 没入モード（HUD が消えている間）だけ画面下部中央に現れる
    プラネタリウム式の字幕。ワープ中は行き先、到着時は天体名・種別・距離・惹句（未知の銀河の初到達には
    「🔭 新発見！」）、シアター中は億年カウンター、ダイブ中はナレーション段階と計器を流す。
    操作ヒント1行を薄く添え、来場者に「触っていい」ことを伝える。暗転（`.dive-fade`）・突入後の
    最終カード（`.dive-final`）・シネマ字幕は没入フェードの例外（無人運転のダイブが真っ黒画面で
    終わる問題の修正）。
  - **操作性**: 天体ホバーでカーソルが pointer になりグローが1.22倍に（クリック可能だと伝わる。
    レイキャストは約0.1秒間隔に間引き）。ダブルクリックで即ワープ。ESC でシアター/ダイブから脱出。
    航行チャートは選択項目へ自動スクロール。Space キーの再生トグルは大宇宙モード中は無効
    （太陽系の再生状態を裏で変えないため）。
  - **没入演出**: ワープ中は FOV を 58→最大74 へ広げて加速感を出す（明るさには不介入）。
    没入モード中は OrbitControls の autoRotate（0.16）でカメラがゆっくり周回し、放置中も画が生きる。
  - **サウンド（`src/cosmos/cosmosAudio.ts`）**: Web Audio だけで合成（外部アセットなし）。
    宇宙アンビエント（55Hz+完全5度のドローン＋ろ過ノイズ）、ワープのバンドパス・スイープ、
    到着ピング、発見ベル、上演開始の低い唸り。上部の「🔇 サウンド」ボタンでトグル、
    自動再生制限があるためデフォルト OFF。
  - **見た目**: フォーム部品の font-family 継承、ダークテーマの細スクロールバー、ボタンの
    :active/:focus-visible、チャート領域見出しの sticky 化、シアター/ダイブ/最終カードの
    入場アニメーション、タブタイトル・絵文字 favicon（data URI）・meta description。
- **検証メモ**: `npm run build` は型エラーゼロで通ること。実行時、シーン構築後の WebGL エラーが 0
  （`gl.getError() === 0`）であること、ワープ／スキャンで `focused` が目標天体になり航行記録が更新される
  こと。シーンの全オブジェクト数は 77（背景2＋curated 26＋UDG 48＋ワープ演出1）＋シアター起動後に+1。
  シアターは「接近→すれ違い（跳ね返り）→再突入→減衰振動→合体」の分離距離カーブになること。
